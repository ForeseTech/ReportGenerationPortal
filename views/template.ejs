<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>STUDENT REPORT | MOCK PLACEMENTS 2021</title>
  </head>
  <body>
    <p>Register Number : <%= student.register_num %></p>
    <p>Name : <%= student.name %></p>
    <p>Department : <%= student.dept %></p>
    <% if(student.section) { %>
    <p>Section : <%= student.dept %></p>
    <% } %>
    <p>E-Mail ID : <%= student.email %></p>
  </body>
</html>
